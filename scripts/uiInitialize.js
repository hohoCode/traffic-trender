// Initialize UI system

// Contributors:
// Richard B. Johnson

$(document).ready(function() {
    uiTreeMap.init();
    uiFilter.init("backend/sources/filter_menu_data.json");
    uiLineGraph.init();
});
